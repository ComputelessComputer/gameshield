{
  "name": "gameshield-api",
  "version": "0.1.0",
  "description": "GameShield CAPTCHA API",
  "main": "index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "start": "bun run dist/index.js",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^3.5.0",
    "@hono/zod-validator": "^0.1.8",
    "security-utils": "workspace:*",
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "bun-types": "^1.0.3",
    "typescript": "^5.0.4"
  }
}
